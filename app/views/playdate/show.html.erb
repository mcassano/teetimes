Date: <%= @date %><br/>
Showing 18 holes with slots available earlier than some time in the afternoon<br/><br/>
<div class="container">
  <div class="row">
    <div class="col-sm">
      <h4><a href="https://app.membersports.com/book-linked-clubs-tee-time/3660/4711/1/1">City of Denver</a></h4>
      <ul>
      <% @city_of_denver.each do |opening| %>
        <li>
          <%= opening['name'] %> <%= minutes_since_midnight_to_time(opening['teeTime']) %> #<%= 4 - opening['playerCount'] %>
        </li>
      <% end %>
      </ul>
    </div>
    <div class="col-sm">
      <h4><a href="https://www.golflongmont.com/utecreek-tee-times">Ute Creek</a></h4>
      <ul>
      <% @ute_creek.each do |opening| %>
        <li><%= opening["teetime"] %> #<%= opening["slots_avail"] %></li>
      <% end %>
      </ul>
    </div>
    <div class="col-sm">
      <h4><a href="https://www.foothillsgolf.org/teetimes/foothills-championship-18/">Foothills Championship 18</a></h4>
      <ul>
      <% @foothillschamp.each do |opening| %>
        <li><%= opening["teetime"] %> #<%= opening["slots_avail"] %></li>
      <% end %>
      </ul>
    </div>
    <div class="col-sm">
      <h4><a href="https://www.golfhylandhills.com/teetimes/">Hyland Hills Gold 18</a></h4>
      <ul>
      <% @hylandhills.each do |opening| %>
        <li><%= opening["teetime"] %> #<%= opening["slots_avail"] %></li>
      <% end %>
      </ul>
    </div>
    <div class="col-sm">
      <h4><a href="https://www.buffalorungolfcourse.com/teetimes/">Buffalo Run</a></h4>
      <ul>
      <% @buffalo.each do |opening| %>
        <li><%= opening["teetime"] %> #<%= opening["slots_avail"] %></li>
      <% end %>
      </ul>
    </div>
  </div>
</div>
